---
title: 'Update Shipment'
---

import ShipmentIdField from '/snippets/api/url-parameters/shipment-id.mdx';

### Overview

This endpoint can be used to update a shipment.

Endpoint: `/api/v1/shipments/{shipment_id}`

Method: `PATCH`

<Warning>
    It's advised you use the [Verify Address](/guide/api/addresses/verify) endpoint to ensure that the fulfillment address and destination address are valid before creating the shipment.

    Failure to verify the addresses may result in the shipment being lost, delayed, or extra charges being applied.
</Warning>

### URL Parameters

<ShipmentIdField action="update"/>

### Request

<ResponseField name="title" type="string" required>
    The name or a brief description of the piece. You can also use an internal reference number.

    Minimum character length: `2`

    Maximum character length: `50`
</ResponseField>

<ResponseField name="value" type="integer" required>
    The value of the artwork in USD $.

    Always use an accurate value to make sure that any insurance claims are properly honored.

    Minimum value: `50`

    Maximum value: `10000`
</ResponseField>

<ResponseField name="length" type="integer" required>
    The length of the piece in inches.

    Minimum length: `17`

    Maximum length: `44`
</ResponseField>

<ResponseField name="width" type="integer" required>
    The width of the piece in inches.

    Minimum width: `13`

    Maximum width: `34`
</ResponseField>

<ResponseField name="height" type="integer" required>
    The height of the piece in inches.

    Minimum height: `1`

    Maximum height: `4`
</ResponseField>

<ResponseField name="service" type="integer" required>
    A [Service](/guide/enums/service) enum representing the service to be used for the shipment.
</ResponseField>

<ResponseField name="fulfillment_address" type="array" required>
    An object containing the fulfillment address details.

    <Expandable title="fulfillment_address" defaultOpen="true">
        <ResponseField name="name" type="string" required>
            The name of the fulfillment address. For example, "John Smith" or "Acme Products".

            Minimum character length: `2`

            Maximum character length: `50`
        </ResponseField>

        <ResponseField name="email" type="string" required>
            The contact email for the fulfillment address.

            Carriers may send one or more shipment tracking notifications to this email address.

            Minimum character length: `6`

            Maximum character length: `100`
        </ResponseField>

        <ResponseField name="phone" type="string" required>
            The contact number for the fulfillment address.

            This must be formatted using the [E.164 standard](http://artport.test/support/articles/formatting-telephone-numbers).

            Minimum character length: `7`

            Maximum character length: `30`
        </ResponseField>

        <ResponseField name="line_1" type="string" required>
            Line 1 of the fulfillment address.

            Minimum character length: `2`

            Maximum character length: `35`
        </ResponseField>

        <ResponseField name="line_2" type="string">
            Line 2 of the fulfillment address.

            Minimum character length: `2`

            Maximum character length: `35`
        </ResponseField>

        <ResponseField name="city" type="string" required>
            The city of the fulfillment address.

            Minimum character length: `2`

            Maximum character length: `35`
        </ResponseField>

        <ResponseField name="region" type="string" required>
            The region of the fulfillment address.

            Minimum character length: `2`

            Maximum character length: `30`
        </ResponseField>

        <ResponseField name="post_code" type="string" required>
            The post code of the fulfillment address.

            Minimum character length: `2`

            Maximum character length: `16`
        </ResponseField>

        <ResponseField name="country" type="integer" required>
            A [Country](/guide/enums/countries) enum representing the fulfillment address' country.
        </ResponseField>
    </Expandable>
</ResponseField>

<ResponseField name="destination_address" type="array" required>
    An object containing the destination address details.

    <Expandable title="destination_address" defaultOpen="true">
        <ResponseField name="name" type="string" required>
            The name of the destination address. For example, "John Smith" or "Acme Products".

            Minimum character length: `2`

            Maximum character length: `50`
        </ResponseField>

        <ResponseField name="email" type="string" required>
            The contact email for the address.

            Carriers may send one or more shipment tracking notifications to this email address.

            Minimum character length: `6`

            Maximum character length: `100`
        </ResponseField>

        <ResponseField name="phone" type="string" required>
            The contact number for the address.

            This must be formatted using the [E.164 standard](/guide/concepts/formatting#phone-numbers).

            Minimum character length: `7`

            Maximum character length: `30`
        </ResponseField>

        <ResponseField name="line_1" type="string" required>
            Line 1 of the destination address.

            Minimum character length: `2`

            Maximum character length: `35`
        </ResponseField>

        <ResponseField name="line_2" type="string">
            Line 2 of the destination address.

            Minimum character length: `2`

            Maximum character length: `35`
        </ResponseField>

        <ResponseField name="city" type="string" required>
            The city of the destination address.

            Minimum character length: `2`

            Maximum character length: `35`
        </ResponseField>

        <ResponseField name="region" type="string" required>
            The region of the destination address.

            Minimum character length: `2`

            Maximum character length: `30`
        </ResponseField>

        <ResponseField name="post_code" type="string" required>
            The post code of the destination address.

            Minimum character length: `2`

            Maximum character length: `16`
        </ResponseField>

        <ResponseField name="country" type="integer" required>
            A [Country](/guide/enums/countries) enum representing the destination address' country.
        </ResponseField>
    </Expandable>
</ResponseField>

### Response

If the shipment is successfully updated, the API will return a `204 No Content` status code.

<RequestExample>
    ```json Example Request Body Structure
    {
        "title": "Test Shipment",
        "value": 1000,
        "length": 17,
        "width": 13,
        "height": 1,
        "service": 1,
        "fulfillment_address": {
            "name": "Schaefer, Nolan and Wehner",
            "email": "dave45@gmail.com",
            "phone": "+12069536978",
            "line_1": "492 Quitzon Avenue Apt. 157",
            "line_2": "Apt. 027",
            "city": "Gustside",
            "region": "Massachusetts",
            "post_code": "17594-7013",
            "country": 1
        },
        "destination_address": {
            "name": "Krajcik, Kuhn and Eichmann",
            "email": "teresa93@gmail.com",
            "phone": "+12069536978",
            "line_1": "1712 Marina Ramp Suite 512",
            "line_2": "Apt. 235",
            "city": "Port Marlon",
            "region": "Indiana",
            "post_code": "62893",
            "country": 1
        }
    }
    ```
</RequestExample>

<ResponseExample>
    ```text 204
    No Content
    ```
</ResponseExample>